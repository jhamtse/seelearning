# Postgres connection string (e.g. from Neon: https://neon.com — free tier, no card required)
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Set automatically when you connect a Vercel Blob store to your project.
# Only needed locally if you want photo uploads to work outside Vercel.
# BLOB_READ_WRITE_TOKEN=
